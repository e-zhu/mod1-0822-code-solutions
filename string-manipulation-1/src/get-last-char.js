/* exported getLastChar */
// find index for last character
// return value of last character
function getLastChar(string) {
  var lastIndex = string.length - 1;
  var lastChar = string.charAt(lastIndex);
  return lastChar;
}
